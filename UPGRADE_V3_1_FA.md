# ارتقای Accounting CRM به نسخه 3.1

## امکانات جدید لیست‌ها
- ذخیره تنظیمات ستون‌ها برای هر کاربر داخل MySQL
- نمایش/عدم نمایش ستون‌ها از پنل چرخ‌دنده
- جابه‌جایی ستون‌ها با Drag & Drop مستقیم روی عنوان ستون
- جابه‌جایی ستون‌ها از پنل تنظیمات ستون
- تغییر عرض ستون با کشیدن مرز عنوان ستون
- امکان وارد کردن عدد عرض ستون در پنل تنظیمات
- دکمه «نمایش همه»
- دکمه «بازگشت به پیش‌فرض»
- پشتیبانی خودکار از فیلدهای اضافه‌ای که بعداً ساخته می‌شوند
- تنظیم مستقل برای لیست شرکت‌ها، سامانه‌ها، برنامه روزانه، برنامه ماهانه و فیلدهای اضافه

## کانبان
کارت برنامه‌های ماهانه را می‌توان بین ستون‌های وضعیت Drag & Drop کرد.
پس از Drop، وضعیت با درخواست AJAX در دیتابیس ذخیره می‌شود و شمارنده ستون‌ها به‌روز می‌شود.

## دیتابیس
Migration جدول زیر را به‌صورت خودکار ایجاد می‌کند:

`user_table_preferences`

نیازی به اجرای ALTER TABLE دستی یا phpMyAdmin نیست.

## نصب
Patch نسخه 3.1 را از Root پروژه اجرا کنید و سپس Commit/Push کنید.
بعد از Deploy در cPanel، یک‌بار `migrate_v2.php` را اجرا کنید.
